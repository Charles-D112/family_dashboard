<div class="container w-50 mt-5 mb-5">
  <h1><%= @tuto.name %></h1>
    <p> <strong>Description :</strong> <%=  @tuto.description %></p>
    <p> <% if @tuto.url.present? %> URL : <%= link_to @tuto.url, @tuto.url %><% end %></p>

      <% @tuto.photos.each do |photo| %>
        <p class="mt2"><%= cl_image_tag photo.key, height: 200, width: 200 %></p>
      <% end %>


  <%= link_to "Modifier le tuto", edit_tuto_path %>
  <br>
  <%= link_to "Supprimer le tuto", tuto_path(@tuto), data: { turbo_method: :delete, turbo_confirm: "Voulez-vous supprimer le tuto définitivement ?" } %>
  <br>
  <%= link_to "Retour aux tutos", tutos_path %>
</div>
